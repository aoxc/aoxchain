# Architecture

- `aoxc`: canonical operational binary
- `make`: build, packaging, and workflow orchestration surface
- `aoxchub`: local transparent operator control plane

The hub previews commands, requires explicit confirmation, executes only approved command specifications, and streams terminal output to the local UI.
