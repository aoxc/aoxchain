# AOXC Hub

Local-only AOXC operator console.

## Design Goals
- Localhost-only control surface
- Transparent command preview before execution
- Immutable command catalog
- Secure orchestration of `aoxc` and approved `make` targets
- Embedded UI assets
- User-oriented operation without requiring terminal fluency
